import React from 'react'
import { itemList } from '../api/Items'

const rough = () => {
  return (
    <div>
        {itemList.map((data,i)=>{
            return(
                <div>
                    
                </div>
            )
        })}
      
    </div>
  )
}

export default rough
